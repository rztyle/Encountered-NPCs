# Encountered NPCs OG

Minimal SillyTavern extension with only:

- Name
- Status
- Relationship
- Manual local chat scan

The scanner reads only the currently loaded SillyTavern chat and never calls an AI model or external API.
